window.onload = function() {

  var canvas        = 'game',
      canvasWidth   = 480,
      canvasHeight  = 640;

  var renderer = new AURA.Renderer(canvas, canvasWidth, canvasHeight);
  renderer.init();
};